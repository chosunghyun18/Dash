spring:
  application:
    name: dash
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

# 소셜 로그인 client-id — 비어있으면 local: 토큰 서명검증 비활성(claims 파싱만, WARN 로그)
apple:
  client-id: ${APPLE_CLIENT_ID:}
google:
  client-id: ${GOOGLE_CLIENT_ID:}
